<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>

    <!--
        Main app theme after the splash screen finishes.
        CRITICAL: We explicitly set android:windowBackground AND android:background
        to the system colorBackground so that the splash drawable does NOT leak
        into system dialogs (date picker, permission prompts), text selection handles,
        or any other native UI element.
    -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
        <!-- Kill the splash drawable at every level -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:background">@null</item>
        <item name="android:colorBackground">?android:attr/colorBackground</item>
        <!-- Fix text selection handles showing with black background -->
        <item name="android:textColorHighlight">#4D1976D2</item>
        <!-- Dialog theme override to ensure clean backgrounds -->
        <item name="android:alertDialogTheme">@style/AppTheme.Dialog</item>
        <item name="alertDialogTheme">@style/AppTheme.Dialog</item>
        <item name="android:datePickerDialogTheme">@style/AppTheme.Dialog</item>
        <item name="android:timePickerDialogTheme">@style/AppTheme.Dialog</item>
    </style>

    <!-- Clean dialog theme without splash artifacts -->
    <style name="AppTheme.Dialog" parent="Theme.AppCompat.DayNight.Dialog.Alert">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:background">@null</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--
        Splash screen launch theme.
        Uses windowSplashScreenBackground (the correct SplashScreen API attribute)
        instead of the old android:background to prevent bleed.
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/colorPrimaryDark</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>